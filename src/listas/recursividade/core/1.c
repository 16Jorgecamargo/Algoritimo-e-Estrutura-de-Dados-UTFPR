#include <stdio.h>
#include "../headlers/1.h"

// Exemplo de implementacao para questao 1 de recursividade
// Esta e apenas uma estrutura de exemplo - implementacao real sera feita pelo aluno

int inverterNumero(int numero) {
    // Implementacao da logica recursiva aqui
    // Esta funcao sera implementada pelo aluno
    printf("Funcao ainda nao implementada!\n");
    return numero;
}

void executarQuestaoRecursividade1(void) {
    printf("Esta questao ainda nao foi implementada.\n");
    printf("Implemente a funcao inverterNumero() para resolver.\n");
    
    // Exemplo de uso futuro:
    // int numero;
    // printf("Digite um numero inteiro positivo: ");
    // scanf("%d", &numero);
    // int resultado = inverterNumero(numero);
    // printf("Numero original: %d\n", numero);
    // printf("Numero invertido: %d\n", resultado);
}